spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/jpashop;NON_KEYWORDS=USER
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
#        show_sql: true
        format_sql : true
logging:
  level:
    #org.springframework.security: DEBUG
    org.hibernate.SQL: debug
    org.hibernate.type: trace
jwt:
  secret: hello.hsbusngo.mynameissuno

