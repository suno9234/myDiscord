
const ReceiverInfoTab = ({ isVisible }) => {

  return (
    <div style={{
      display: isVisible ? 'flex' : 'none',
      width: '200px',
      height: '100%',
    }}>

    </div>
  )
}

export default ReceiverInfoTab;